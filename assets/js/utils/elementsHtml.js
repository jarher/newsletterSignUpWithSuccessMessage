const newsletter__container = document.querySelector(".newsletter__container");

const newsletter__message__container = document.querySelector(
  ".newsletter__message-notification--container"
);

const newsletter_message_button = document.querySelector(
  ".newsletter__message-notification--button"
);

const elementsHtml = {
  newsletter__container,
  newsletter__message__container,
  newsletter_message_button,
};

export default elementsHtml;
